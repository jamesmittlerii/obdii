name: Swift IOS Fastlane Workflow

permissions:
      contents: write
      pull-requests: write
      id-token: write

on:
  workflow_dispatch:
  push:
    branches: [ "main" ]
    paths-ignore:
      - "flutter_obdii/**"
      - ".github/workflows/caller_flutter.yml"
#  pull_request:
#    branches: [ "main" ]


concurrency:
  group: ${{ github.repository }}-ios-release-${{ github.ref }}
  cancel-in-progress: false

jobs:
  call-reusable-workflow:
    uses: jamesmittlerii/workflows/.github/workflows/iosnotest_bw.yml@main # Or a specific commit SHA or tag
    secrets: inherit
