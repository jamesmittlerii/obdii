# Keep these standard settings
#org.gradle.jvmargs=-Xmx4g -Dfile.encoding=UTF-8
org.gradle.jvmargs=-Xmx2048m -XX:MaxMetaspaceSize=512m -Dfile.encoding=UTF-8
android.useAndroidX=true
kotlin.code.style=official

# Add this to resolve the performance warning from your logs
#android.dependency.excludeLibraryComponentsFromConstraints=true
# Remove the 'excludeLibraryComponents' line and replace it with:
android.dependency.useConstraints=false
# Keep this only if your build breaks without it (R8 Full Mode is stricter)
android.r8.strictFullModeForKeepRules=false

# --- DELETED ALL DEPRECATED/LEGACY FLAGS BELOW ---

# Enable configuration caching for faster builds
org.gradle.configuration-cache=true
