# Marketing semver for Flutter (before +), Kotlin androidApp versionName, and Swift obdii MARKETING_VERSION.
# Bump this when cutting a release; run: python3 scripts/sync_release_version.py (or rely on CI sync).
1.0.2
